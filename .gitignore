si
si-linux-amd64
si-chat/node_modules
si-chat/dist

# inber agent-session runtime state (session/memory DBs + materialized workspace);
# never source. Matches the canonical inber repo, which gitignores .inber/.
.inber/
